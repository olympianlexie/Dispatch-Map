{
  "name": "dispatch-map",
  "version": "0.1.0",
  "private": true,
  "description": "Live dispatch map for open installation JOs and Cartrack vehicle locations.",
  "scripts": {
    "dev": "netlify dev",
    "test": "node --test"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
